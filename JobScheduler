<!DOCTYPE html>
<html>
<head>
  <title>Barclays-Style QR Code</title>
</head>
<body>
<canvas id="qrCanvas"></canvas>
<img id="logo" src="logo.png" style="display:none;"/>

<script src="https://cdn.jsdelivr.net/npm/qrcode-generator@1.4.4/qrcode.js"></script>
<script>
function drawBarclaysStyleQRCode(url) {
    const qr = qrcode(4, 'H'); // High error correction
    qr.addData(url);
    qr.make();

    const canvas = document.getElementById('qrCanvas');
    const ctx = canvas.getContext('2d');

    const moduleCount = qr.getModuleCount();
    const size = 300;
    const moduleSize = size / moduleCount;
    canvas.width = size;
    canvas.height = size;

    // Fill background white
    ctx.fillStyle = "#FFFFFF";
    ctx.fillRect(0, 0, size, size);

    // Draw QR data modules (black squares)
    for (let row = 0; row < moduleCount; row++) {
        for (let col = 0; col < moduleCount; col++) {
            if (qr.isDark(row, col)) {
                const inCorner = 
                    (row < 7 && col < 7) || 
                    (row < 7 && col >= moduleCount - 7) ||
                    (row >= moduleCount - 7 && col < 7);
                if (!inCorner) {
                    ctx.fillStyle = "#000000"; // Black modules
                    ctx.fillRect(col * moduleSize, row * moduleSize, moduleSize, moduleSize);
                }
            }
        }
    }

    // Function to draw Starbucks-style corners with custom color
    function drawCorner(x, y, color) {
        const outer = 7 * moduleSize;
        const inner = 3 * moduleSize;
        const offset = 2 * moduleSize;

        ctx.fillStyle = color; // Barclays blue
        ctx.fillRect(x, y, outer, outer); // Outer square

        ctx.fillStyle = "#FFFFFF"; // Middle white
        ctx.fillRect(x + moduleSize, y + moduleSize, outer - 2*moduleSize, outer - 2*moduleSize);

        ctx.fillStyle = color; // Inner square
        ctx.fillRect(x + offset, y + offset, inner, inner);
    }

    // Draw three corners
    const barclaysBlue = "#0061BA"; // Barclays blue
    drawCorner(0, 0, barclaysBlue); // top-left
    drawCorner(size - 7*moduleSize, 0, barclaysBlue); // top-right
    drawCorner(0, size - 7*moduleSize, barclaysBlue); // bottom-left

    // Draw white center for logo
    const logo = document.getElementById('logo');
    logo.onload = function() {
        const logoSize = size * 0.25;
        const center = size / 2;

        ctx.fillStyle = "#FFFFFF"; // white block
        ctx.fillRect(center - logoSize/2, center - logoSize/2, logoSize, logoSize);

        // Draw logo image
        ctx.drawImage(logo, center - logoSize/2, center - logoSize/2, logoSize, logoSize);
    }
    logo.src = "logo.png"; // your logo path
}

// Generate QR code
drawBarclaysStyleQRCode("https://your-url.com");
</script>
</body>
</html>
